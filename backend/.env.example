DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_database_password
DB_NAME=your_database_name
JWT_SECRET=replace_with_a_long_random_secret

# Recommendation uses the local product catalog; no AI API key is required.
PORT=5000
